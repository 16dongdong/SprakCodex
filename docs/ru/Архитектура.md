#АРХИТЕКТУРА

В этом документе описываются текущая структура репозитория SprakCodex, связи между частями системы и релизный контур. Его цель — помочь участникам быстро понять, в какой слой проекта нужно вносить изменения.

## 1. Общая форма

SprakCodex состоит из двух типов режимов работы:

1. Режим рабочего стола: Tauri Рабочий стол + локальный сервисный процесс
2. Режим Service: автономный сервис + веб-интерфейс, может использоваться с сервером, Docker или без среды рабочего стола.

Единая цель:

- Управление учетными записями, использованием и ключами платформы
- Обеспечение возможностей локального шлюза
- Внешне совместим с записью в стиле OpenAI и адаптирован к нескольким восходящим протоколам.

## 2. Структура и обязанности директории

```text
.
├─ frontend/              # Next.js 前端与 Tauri 桌面壳
│  ├─ src/                # 页面、组件、状态与 API 客户端
│  ├─ src-tauri/          # 桌面原生命令和打包配置
│  └─ tests/              # 前端回归
├─ backend/               # 独立 Rust 工作区
│  ├─ crates/             # core、rusqlite、service、web、start
│  ├─ scripts/            # 构建和发行脚本
│  └─ docker/             # 容器部署与代理配置
├─ docs/                  # 文档、assets 图片与 skills 示例
└─ .github/               # CI 与发布流程
```

## 3. Базовый комплексный индекс записи домена

### 3.1 Главный вход внешнего управления

- `frontend/src/main.js`: Вход в стартовую сборку внешнего интерфейса
- `frontend/src/runtime/app-bootstrap.js`: Порядок инициализации интерфейса
- `frontend/src/runtime/app-runtime.js`: Координация процесса обновления и времени выполнения.
- `frontend/src/settings/controller.js`: Настройка фасада домена и продолжение распространения по подмодулям

### 3.2 Вход в оболочку рабочего стола

- `frontend/src-tauri/src/lib.rs`: Tauri запись сборки приложения
- `frontend/src-tauri/src/settings_commands.rs`: Команда моста настройки рабочего стола
- `frontend/src-tauri/src/service_runtime.rs`: Жизненный цикл встроенных услуг настольных компьютеров
- `frontend/src-tauri/src/rpc_client.rs`: Рабочий стол RPC Инфраструктура вызовов

### 3.3 сервисный шлюз и запись протокола

- `backend/crates/service/src/lib.rs`: Главный вход для обслуживания и сборка во время выполнения
- `backend/crates/service/src/http/`: запись маршрутизации HTTP
- `backend/crates/service/src/rpc_dispatch/`: RPC Вход для раздачи
- `backend/crates/service/src/gateway/mod.rs`: запись агрегации шлюза
- `backend/crates/service/src/gateway/observability/http_bridge.rs`: отслеживание запросов, соединение протоколов, запись журналов.
- `backend/crates/service/src/gateway/protocol_adapter/request_mapping.rs`: Сопоставление входных данных OpenAI/Codex
- `backend/crates/service/src/gateway/protocol_adapter/response_conversion.rs`: запись об общем количестве конверсий результатов, не связанных с потоковой передачей.
- `backend/crates/service/src/gateway/protocol_adapter/response_conversion/sse_conversion.rs`: Потоковая запись SSE о преобразовании
- `backend/crates/service/src/gateway/protocol_adapter/response_conversion/openai_chat.rs`: OpenAI Адаптация результатов чата
- `backend/crates/service/src/gateway/protocol_adapter/response_conversion/tool_mapping.rs`: сокращение и восстановление имени инструмента

### 3.4 Настройка и запуск записи конфигурации

- `backend/crates/service/src/app_settings/`: настройка постоянства, охвата переменных среды, синхронизации времени выполнения.
- `backend/crates/service/src/web_access.rs`: Web Пароль доступа и токен сеанса

## 4. Текущие отношения

### 4.1 Режим рабочего стола

Режим рабочего стола состоит из следующих частей:

- `frontend/src/`: Интерфейсный интерфейс
- `frontend/src-tauri/`: Оболочка рабочего стола
- `backend/crates/service/`: местная служба

Как запустить:

1. Пользователь запускает настольное приложение.
2. Tauri Оболочка отвечает за поведение рабочего стола, такое как окна, лотки, обновления, отдельные экземпляры и настройка мостов.
3. Рабочий стол связывается с `codexmanager-service` через RPC или локальный адрес.
4. Интерфейсный интерфейс отображает такие страницы, как учетная запись, использование, журнал запросов и настройки.

### 4.2 Режим Service

Шаблон Service состоит из следующих двоичных файлов:

- `codexmanager-service`
- `codexmanager-web`
- `codexmanager-start`

Обязанности:

- `codexmanager-service`: Основной сервисный процесс, обеспечивающий управление учетными записями, переадресацию шлюза, журналы запросов, постоянство настроек и интерфейсы RPC/HTTP.
- `codexmanager-web`: Web Сервисная оболочка пользовательского интерфейса, которая может напрямую предоставлять интерфейсные страницы и прокси-сервер локальным службам.
- `codexmanager-start`: Средство запуска одним щелчком мыши для публикации пакетов, отвечающее за одновременный запуск сервиса и Интернета.

## 5. Обязанности модуля

### 5.1 `frontend/src/`

Основная ответственность за:

- Рендеринг страницы
- взаимодействие с пользователем
- Управление статусами
- Вызов локального API/команды Tauri
- Интерфейсная логика страницы настроек и страницы учетной записи

### 5.2 `frontend/src-tauri/`

Основная ответственность за:

- Tauri Запуск приложения
- Управление одним экземпляром
- События в системном трее и окнах
- Обновления рабочего стола и поведение установщика
- Объедините интерфейсные операции со службой/локальной средой выполнения.

### 5.3 `backend/crates/core/`

Основная ответственность за:

- SQLite Миграция
- Базовые возможности хранилища
- Основная базовая логика, такая как аутентификация/использование.
- Возможности доступа к данным, которые могут повторно использоваться службами.

### 5.4 `backend/crates/service/`

Основная ответственность за:

- HTTP / RPC Портал
- Учетная запись, использование, управление ключами API
- Возможности локального шлюза
- Адаптация протокола и восходящая пересылка
- Запросить ведение журнала и настроить постоянство
- Синхронизация конфигурации времени выполнения

Ключевые подкаталоги:

- `src/gateway/`: Шлюз, адаптация протокола, потоковое и непотоковое преобразование
- `src/http/`: запись маршрутизации HTTP
- `src/rpc_dispatch/`: RPC Распространение
- `src/account/`, `src/apikey/`, `src/requestlog/`, `src/usage/`: Логика предметной области

### 5.5 `backend/crates/web/`

Основная ответственность за:

- Предоставьте статические ресурсы пользовательского интерфейса Web.
- Монтирование или прокси-сервер для службы
- При желании вставьте `frontend/dist` в двоичный файл, чтобы сформировать однофайловый дистрибутив.

### 5.6 `backend/crates/start/`

Основная ответственность за:

- Предоставьте более прямую запись запуска в пакете выпуска Service.
- Координировать жизненный цикл сервиса и сети

## 6. Данные и конфигурация

### 6.1 База данных

В текущем проекте используется SQLite.
Миграция базы данных находится по адресу:

- `backend/crates/core/migrations/`

База данных не только хранит учетные записи, но и предполагает:

- API-ключ
- Запросить журнал
- статистика токенов
- настройки приложения

### 6.2 Запуск конфигурации

К основным источникам конфигурации относятся:

- Переменные среды `CODEXMANAGER_*`
- `.env` / `codexmanager.env` в каталоге запуска приложения
- `app_settings` Таблица устойчивости
- Страница настроек рабочего стола

Текущее соглашение:

- Конфигурации, которые должны вступить в силу перед запуском, сохраняются на уровне переменных среды.
- Настраиваемые конфигурации во время выполнения сначала управляются через страницу настроек + `app_settings`.
- Изменения настроек не должны без границ разбрасываться по рабочим столам, интерфейсам и службам.

## 7. Запросить обзор ссылки

Типичные ссылки на запросы следующие:

1. Клиент или пользовательский интерфейс инициирует запрос.
2. Запросы поступают на уровень HTTP/RPC `backend/crates/service`.
3. Модуль шлюза определяет стратегию пересылки, номер счета, стратегию заголовка, восходящий прокси-сервер и т. д.
4. 协议适配层负责处理：
   - `/v1/chat/completions`
   - `/v1/responses`
   - Потоковая передача SSE
   - Непотоковый JSON
   - `tool_calls` / инструменты сопоставления и агрегирования
5. Результаты записываются обратно в журнал запросов и статистику, а затем возвращаются вызывающей стороне.

## 8. Создание и публикация ссылок

### 8.1 Локальная разработка и постройка

внешний интерфейс:

- `pnpm -C frontend run dev`
- `pnpm -C frontend run build`
- `pnpm -C frontend run check`

Ржавчина:

- `cargo test --manifest-path backend/Cargo.toml --workspace`
- `cargo build --manifest-path backend/Cargo.toml -p codexmanager-service --release`
- `cargo build --manifest-path backend/Cargo.toml -p codexmanager-web --release`
- `cargo build --manifest-path backend/Cargo.toml -p codexmanager-start --release`

Рабочий стол:

- `backend/scripts/rebuild.ps1`
- `backend/scripts/rebuild-linux.sh`
- `backend/scripts/rebuild-macos.sh`

### 8.2 Управление версиями

В настоящее время версия поддерживается единообразно в корневой рабочей области:

- Корневой `backend/Cargo.toml` `[workspace.package].version`

Дополнительная синхронизация на рабочем столе:

- `frontend/src-tauri/Cargo.toml`
- `frontend/src-tauri/tauri.conf.json`

Единая запись модификации:

- `backend/scripts/bump-version.ps1`

### 8.3 GitHub Релиз

Главный вход в издательство:

- `.github/workflows/release-all.yml`

Обязанности:

- Создайте настольный продукт Windows / macOS / Linux
- Артефакт версии Service сборки
- Загрузить GitHub Выпустить прикрепленный файл
- Определите тип выпуска на основе введенного тега / `prerelease`.

## 9. Текущие структурные риски

Текущий склад должен сосредоточиться на следующих вопросах:

1. `frontend/src-tauri/src/lib.rs` Он все еще толстый, и сборку оболочки рабочего стола и реализацию команд все еще необходимо разобрать.
2. `backend/crates/service/src/lib.rs` Конфигурация, синхронизация времени выполнения и границы побочных эффектов недостаточно ясны.
3. `backend/crates/service/src/gateway/protocol_adapter/response_conversion.rs` Существует множество совместимых ветвей, и риск регресса высок.
4. `.github/workflows/release-all.yml` Тем не менее, длинная многоплатформенная логика требует ограничений постоянства.

## 10. Предлагаемые изменения

Чтобы уменьшить структурное загрязнение, новые требования должны быть ориентированы в соответствии со следующими принципами:

- Новые страницы или взаимодействие с интерфейсом: приоритет попадает в `frontend/src/views/`, `frontend/src/services/`, `frontend/src/ui/`.
- Новые возможности рабочего стола: отдавайте приоритет автономным модулям, попадающим в `frontend/src-tauri/src/`, вместо того, чтобы продолжать запихивать их все в `lib.rs`.
- Новый элемент настройки: сначала определите, принадлежит ли он переменным среды, постоянной конфигурации или состоянию времени выполнения.
- Совместимость с новыми протоколами: приоритет должен быть отдан подмодулю адаптера шлюза/протокола, и не продолжать складывать условные ветки не по порядку.
- Новая логика выпуска: отдавайте приоритет рисованию сценариев или повторному использованию шагов и не повторяйте изменения три раза на трех платформах.