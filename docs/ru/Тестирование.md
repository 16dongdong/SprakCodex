#ТЕСТИРОВАНИЕ

Этот документ задаёт базовые требования к тестированию и валидации на уровне репозитория CodexManager.

Цель:

- Новые сотрудники могут быстро узнать, что нужно запустить, по крайней мере, после внесения изменений.
– Избегайте обновления всех изменений до полной проверки.
- Обеспечьте фиксированные входы для проверки изменений высокого риска, таких как совместимость протоколов, публикация ссылок и управление настройками.

## 1. Базовая среда

-Node.js 20
- пнпм 9
- Устойчивость к ржавчине
- PowerShell 7+ (Windows Проверка упаковки/скрипта)

## 2. Изменения во внешнем интерфейсе

Область применения:

- `frontend/src/app/`
- `frontend/src/components/`
- `frontend/src/lib/`
- `frontend/src/hooks/`

Минимальная проверка:

``` баш
pnpm -C приложения запускают сборку
pnpm -C приложения запускают тест: время выполнения
```

Описание:

- `pnpm -C frontend run build`: убедитесь, что ссылка статического экспорта Next.js все еще работает.
- `pnpm -C frontend run test:runtime`: Подтвердите определение возможностей во время выполнения и рабочий стол / Web Логика ухудшения возможностей не вернулась
- Если изменения включают идентификацию во время выполнения, Web RPC, обработку различий рабочего стола/Web, составьте Раздел 4.

## 3. Рабочий стол / Tauri Изменения

Область применения:

- `frontend/src-tauri/`
- Обновления рабочего стола, лоток, окно, изменения, связанные с командным мостом.

Минимальная проверка:

``` баш
грузовой тест --workspace
```

Дополнительные предложения:

```powershell
pwsh -NoLogo -NoProfile -File backend/scripts/rebuild.ps1 -Bundle nsis -CleanDist
```

Описание:

- Всякий раз, когда вы меняете Tauri мост или жизненный цикл рабочего стола, рекомендуется хотя бы один раз выполнить проверку сборки локального рабочего стола.

## 4. Web Запуск изменений, совместимых с оболочкой/развертыванием

Область применения:

- `backend/crates/web/`
- `frontend/src/lib/api/transport.ts`
- `frontend/src/components/layout/app-bootstrap.tsx`
- `frontend/src/components/layout/header.tsx`
- `frontend/src/components/layout/sidebar.tsx`
- Web Агент, `/api/runtime`, `/api/rpc` и соответствующие изменения в методах развертывания.

Минимальная проверка:

``` баш
pnpm -C приложения запускают сборку
pnpm -C приложения запускают тест: время выполнения
грузовой тест -p codexmanager-web
pwsh -NoLogo -NoProfile -File backend/scripts/tests/web_runtime_probe.test.ps1
```

Предлагаемые дополнения:

```powershell
pwsh -NoLogo -NoProfile -File backend/scripts/tests/web_runtime_probe.ps1 `
  -База http://localhost:48761
pwsh -NoLogo -NoProfile -Файл скриптов/тестов/web_ui_smoke.ps1 -SkipBuild
```

Описание:

- `pnpm -C frontend run build`: убедитесь, что внешний статический экспорт все еще может быть создан.
- `pnpm -C frontend run test:runtime`: убедитесь, что контракт среды выполнения внешнего интерфейса и определение возможностей согласованы.
- `cargo test --manifest-path backend/Cargo.toml -p codexmanager-web`: Подтвердить Web маршрутизацию оболочки и контракт на проверку времени выполнения.
- `web_runtime_probe.test.ps1`: Подтвердите поведение сценария Web для запуска оболочки с минимальной дымовой ссылкой.
- `web_ui_smoke.ps1`: подтверждение ключевого поведения пользовательского интерфейса страниц Web в поддерживаемых/неподдерживаемых оболочках запуска.

## 5. Изменения на стороне сервера Rust

Область применения:

- `backend/crates/core/`
- `backend/crates/service/`
- `backend/crates/start/`
- `backend/crates/web/`

Минимальная проверка:

``` баш
грузовой тест --workspace
```

Дополнительные предложения:

``` баш
грузовая сборка -p codexmanager-service --release
Cargo build -p codexmanager-web --release
грузовая сборка -p codexmanager-start --release
```

## 6. Адаптация протокола/модификация шлюза

Область применения:

- `backend/crates/service/src/gateway/`
- `backend/crates/service/src/http/`
- `backend/crates/service/src/lib.rs`

Должно охватывать:

- `/v1/responses`
- `/v1/chat/completions`
- Потоковая передача SSE
- Непоточный JSON
- `tools`
- `tool_calls`

Минимальная проверка:

``` баш
грузовой тест --workspace
pwsh -NoLogo -NoProfile -Файл backend/scripts/tests/gateway_reгрессия_suite.ps1
pwsh -NoLogo -NoProfile -Файл скриптов/тестов/codex_stream_probe.ps1
pwsh -NoLogo -NoProfile -Файл скриптов/тестов/chat_tools_hit_probe.ps1
```

Описание:

- Если в локальной среде нет реальной вышестоящей учетной записи, по крайней мере, запустите тест Rust и сохраните инструкции по выполнению проверки.
- Исправление совместимости, из-за которого невозможно было проверить только один тип клиента.

## 7. Настройки/переменные среды/изменения постоянства

Область применения:

- `frontend/src/settings/`
- `backend/crates/service/src/app_settings/`
- `backend/crates/core/src/storage/settings.rs`
- Добавлены элементы конфигурации `CODEXMANAGER_*`.

Минимальная проверка:

``` баш
pnpm -C приложения запускают сборку
грузовой тест --workspace
```

Должно быть подтверждено вручную:

- Значение по умолчанию понятно?
- Стоит ли писать `app_settings`
- Нужно ли синхронизироваться со средой выполнения
- README / `Руководство-для-участников.md` / `Архитектура.md` Нужно ли его обновлять?

## 8. Публикация изменений ссылки

Область применения:

- `.github/workflows/`
- `.github/actions/`
- `backend/scripts/release/`
- `backend/scripts/rebuild*`

Минимальная проверка:

``` баш
pnpm -C приложения запускают сборку
грузовой тест --workspace
pwsh -NoLogo -NoProfile -File backend/scripts/tests/assert-release-version.test.ps1
pwsh -NoLogo -NoProfile -Файл backend/scripts/tests/rebuild.test.ps1
```

Должно быть подтверждено вручную:

- Ввод рабочего процесса соответствует описанию README.
- Нет никаких отклонений в названиях продуктов.
- Никаких отклонений в предварительном выпуске/последнем поведении.

## 9. Изменения в документообороте

Область применения:

- `README*`
- `Архитектура.md`
- `Руководство-для-участников.md`
- `Журнал-изменений.md`
- `docs/`

Минимальная проверка:

- Проверьте, действителен ли путь ссылки.
- Проверьте, не дублируются ли обязанности по документам
- Проверьте, соответствуют ли номер версии, имя продукта и имя рабочего процесса фактическим.

## 10. Минимальные рекомендации по проверке перед отправкой

### Общие изменения

``` баш
pnpm -C приложения запускают сборку
pnpm -C приложения запускают тест: время выполнения
грузовой тест -p codexmanager-web
```

### Изменения на главной странице

``` баш
pnpm -C приложения запускают сборку
pnpm -C приложения запускают тест: время выполнения
```

### Web Изменения совместимости/развертывания

``` баш
pnpm -C приложения запускают сборку
pnpm -C приложения запускают тест: время выполнения
грузовой тест -p codexmanager-web
pwsh -NoLogo -NoProfile -File backend/scripts/tests/web_runtime_probe.test.ps1
```

### Изменения в адаптации протокола

``` баш
грузовой тест --workspace
pwsh -NoLogo -NoProfile -Файл backend/scripts/tests/gateway_reгрессия_suite.ps1
```

## 11. Соглашение о записи результатов

- Проверки, которые могут быть выполнены полностью, записываются как «Выполненные».
- Проверки, которые невозможно выполнить из-за экологических ограничений, четко обозначаются как «не выполнено + причина».
- Не путайте «должен пройти» с «проверено».